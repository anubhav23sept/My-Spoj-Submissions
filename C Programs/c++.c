#include<stdio.h>
#include<string.h>
int main()
{
    long int po,pt,pe;
    while(1)
    {
        cin>>po>>pt>>pe;
        if(po==0 && pt==0 && pe==0)
            break;

    }
    return 0;
}
